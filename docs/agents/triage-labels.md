# Triage labels: Beads workspace pointer

Read the canonical Beads label mapping, `docs/agents/triage-labels.md` in the workspace root (the nearest ancestor directory that contains `docs/agents/git-workflow.md`), before applying triage state. These are Beads labels, not GitHub issue labels.
